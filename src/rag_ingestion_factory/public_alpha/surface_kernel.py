from __future__ import annotations

def public_alpha_surface_kernel_status() -> dict:
    return {
        "version": "v80.0.0",
        "status": "public-alpha-surface-kernel-ready",
        "formula": "Home Polish → Public Navigation → CTA Flow → Route Health → SEO Metadata → Social Preview → Mobile Readiness → Vercel Checks → Public Proof → Launch Pack",
        "canon": "The public alpha now has a surface people can understand, navigate and trust.",
    }
