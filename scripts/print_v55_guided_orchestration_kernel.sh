#!/usr/bin/env bash
set -euo pipefail
echo "━━━━━━━━━━━━━━━━━━━━"
echo "🟢 v55.0.0 — Guided Knowledge Orchestration Kernel"
echo "━━━━━━━━━━━━━━━━━━━━"
echo "Question → Conductor → Retrieval Strategy → Worker Assignment → Context Policy → Evidence Retrieval → Verifier → Human Approval → Retry if Needed → Trusted Knowledge Outcome"
echo "Route: /conductor"
echo "Demo: /conductor-demo"
