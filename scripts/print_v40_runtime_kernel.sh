#!/usr/bin/env bash
set -euo pipefail
echo "━━━━━━━━━━━━━━━━━━━━"
echo "🟢 v40.0.0 — Portable TAP-TAP Runtime Kernel"
echo "━━━━━━━━━━━━━━━━━━━━"
echo "Repo Confirmed → Preflight Guard → Dry Run → Portable Script → Safe Commit Message → Verify → Commit → Tag → Push → Evidence Pack → Incident Note if Needed → Forward-Only Correction"
echo "Route: /release-operator"
echo "API: /api/release-operator/status"
