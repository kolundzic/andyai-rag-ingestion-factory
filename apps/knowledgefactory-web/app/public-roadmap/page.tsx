import { PageShell } from "../components/PageShell";

export default function PublicRoadmapPage() {
  return (
    <PageShell title="Public Roadmap" eyebrow="what exists, what is next">
      <p>Public alpha roadmap: conductor persistence, live dashboard, public proof, pilot workflows, feedback intake and production hardening.</p>
    </PageShell>
  );
}
