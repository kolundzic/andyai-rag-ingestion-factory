import { PageShell } from "../components/PageShell";

export default function PublicFeedbackPage() {
  return (
    <PageShell title="Public Feedback" eyebrow="interest capture">
      <p>Capture public interest, pilot requests, use cases, notes and consent as structured project signals.</p>
    </PageShell>
  );
}
