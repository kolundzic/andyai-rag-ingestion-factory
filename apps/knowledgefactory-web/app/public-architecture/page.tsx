import { PageShell } from "../components/PageShell";

export default function PublicArchitecturePage() {
  return (
    <PageShell title="Public Architecture Overview" eyebrow="governed knowledge system">
      <p>Documents → ingestion → retrieval → KnowledgeBlocks → curator → conductor → verifier → approval → evidence → trusted knowledge.</p>
    </PageShell>
  );
}
