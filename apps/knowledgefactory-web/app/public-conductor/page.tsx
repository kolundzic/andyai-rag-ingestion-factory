import { PageShell } from "../components/PageShell";

export default function PublicConductorPage() {
  return (
    <PageShell title="Conductor Public Explainer" eyebrow="guided orchestration">
      <p>Conductor chooses the path. Worker executes. Verifier checks. Human approves. Evidence remembers. Knowledge Factory canonizes.</p>
    </PageShell>
  );
}
