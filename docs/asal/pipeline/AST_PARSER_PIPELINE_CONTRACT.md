# v103.0.0 — AST Parser Pipeline Contract

Source → Parse → AST → Entities → Relations → Evidence → Graph

AST means Abstract Syntax Tree: a structured representation of code that reveals functions, classes, scopes and calls.
